import { device } from './../../assets/styles';
import styled from 'styled-components';

export const Wrp = styled.div`
    @media ${device.laptop} {
        font-weight: 700;
        font-size: 56px;
        line-height: 72px;
        color: #3877EE;
      }
`